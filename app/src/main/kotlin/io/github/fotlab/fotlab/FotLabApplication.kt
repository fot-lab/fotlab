package io.github.fotlab.fotlab

import android.app.Application

/**
 * Application entry point.
 *
 * Deliberately empty for now: process-wide wiring (database providers) is added
 * here once the corresponding DESIGN items are approved.
 */
class FotLabApplication : Application()
